import React from  "react";
import { StyleSheet, Text, TouchableHighlight, View, Image, Button } from "react-native";
import Teams from "./2SelectTeams";

function Einstellungen({ navigation}){

    return(
        <View title="Einstellungen">
            {/*<Button title="Weiter zu ActualGame" onPress={() =>
            navigation.navigate('ActualGame')}/> */}
        </View>   

        
    )
    
    
    
}
export default Einstellungen;